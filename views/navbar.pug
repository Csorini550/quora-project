
doctype html
html
  head
    <script src="https://kit.fontawesome.com/6e38a041a3.js" crossorigin="anonymous"></script>
    script(src="/javascripts/index.js")
    title="Query"
    //link(rel="stylesheet" href="/stylesheets/reset.css")
    link(rel='stylesheet' href='/stylesheets/navbar.css')
    link(rel='stylesheet' href='/stylesheets/dropdown.css')
    block head
  body
    nav(class='nav')
      img(src="/images/query-logo.svg" class='navbar-logo')
      a(href='/') Home
        i(class="fas fa-home")
      a(href='/users/question') Questions
        i(class="far fa-question-circle")
      a(href='/users/answer') Answers
        i(class="fas fa-book")
      form(id='search-bar' action='/api/search')
        input.search-input(type='text' name='term' placeholder='Search for a question!')
      div(class= 'dropdown') Where am i!!
          button(onclick='logoutUser()' class='dropButton')
          select(id="myDropdown" class="dropdown-content")
            a(href='/users/login') Logout
            a(href='/users/login/edit') Edit Personal info //route does not exist yet - hope to have it so the user can change password or email
            a(href='/users/picture') Add user picture

    block content
